<div class="container-fluid">
  <h1>All Borrowers</h1>
  <a href="/borrowers/new">New Borrower</a>
</div>
<table class="table">
  <tr>
    <th>ID & Profile</th>
    <th>First Name</th>
    <th>Last Name</th>
    <th>Phone Number</th>
    <th>Email</th>
    <th>Address</th>
  </tr>
  <% @borrowers.each do |v| %>
    <tr>
      <td>
        <%= link_to borrower_path(v) do %>
          Profile #<%= v.id %>
        <% end %>
      </td>
      <td><%= v.first_name %></td>
      <td><%= v.last_name %></td>
      <td><%= v.phone_number %></td>
      <td><%= v.email %></td>
      <td><%= v.address %></td>
    </tr>
  <% end %>
</table>
